import XCTest
@testable import SDWebImageFLPlugin

final class SDWebImageFLPluginTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SDWebImageFLPlugin().text, "Hello, World!")
    }
}
